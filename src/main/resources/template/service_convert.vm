package ${packageName}.service.convert;

import java.util.List;

import com.google.common.collect.Lists;
import org.apache.commons.collections.CollectionUtils;
import com.oneplus.service.dto.${upClassName}DTO;
import com.oneplus.dao.model.${upClassName};

${classTitle}
public class ${upClassName}Convert {

    private ${upClassName}Convert() {
    }

    public static ${upClassName} convert(${upClassName}DTO ${lowClassName}DTO) {
        if (${lowClassName}DTO == null) {
            return null;
        }

        ${upClassName} ${lowClassName} = new ${upClassName}();
#foreach($convert in $converts)
        ${convert};
#end

        return ${lowClassName};
    }

    public static List<${upClassName}> convert(List<${upClassName}DTO> ${lowClassName}DTOs) {
        if (CollectionUtils.isEmpty(${lowClassName}DTOs)) {
            return Lists.newArrayList();
        }

        List<${upClassName}> ${lowClassName}s = Lists.newArrayList();
        for (${upClassName}DTO ${lowClassName}DTO : ${lowClassName}DTOs) {
            ${lowClassName}s.add(convert(${lowClassName}DTO));
        }

        return ${lowClassName}s;
    }

}
