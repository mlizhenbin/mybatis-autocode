package ${packageName}.service;

import java.util.List;

${classTitle}
public interface ${upClassName}QueryManager {

	/**
	 * 通过主键${primaryKeyType}查询${upClassName}
	 */
	public Result get${upClassName}ByPrimaryKey(${primaryKeyType} ${primaryKey});

}